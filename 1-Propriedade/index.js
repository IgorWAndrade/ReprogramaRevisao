/*
    let
    var
    const
*/

/*
    Liguagens Tipadas
        e
    Linguagens Não-Tipadas *
*/

//SINTAXE para ESCREVER uma PROPRIEDADE

// Definicao de uma Propriedade começa por um tipo (let,var e const)
// Depois começamos dando um nome para ela

var variavel = 0; 
let variavelEscopo = 0; //Aconselho a utilizar sempre 'let'
const constante = 0;

//Pode utilizar tanto 'let' ou 'var', mas bons profinais não utilizam mais o 'var'
console.log(1 + 1);